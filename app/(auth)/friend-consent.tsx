import FriendConsentFeature from '@/components/friend-consent';

/**
 * 친구 연동 동의 페이지 (라우팅 레이어)
 * 비즈니스 로직 없이 Feature Container만 렌더링
 */
export default function FriendConsent() {
  return <FriendConsentFeature />;
}

