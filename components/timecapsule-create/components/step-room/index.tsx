import { View, Text } from 'react-native';
import { styles } from './styles';

export const StepRoom = (): JSX.Element => {
  return (
    <View style={styles.container}>
      <Text>Step Room Component</Text>
    </View>
  );
};
