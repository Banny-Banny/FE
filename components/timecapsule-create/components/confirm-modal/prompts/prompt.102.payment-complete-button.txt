아래의 조건을 모두 적용하여, 아래의 요구사항을 모두 구현할 것.
구현 결과를 체크리스트로 반환할 것.

==============================================

조건-커서룰) 아래의 커서룰을 적용하여 작업하고, 이 작업이 끝나면 해당 rules 적용 결과를 체크리스트로 반환할 것.
            - @.cursor/rules/01-common.mdc
            - @.cursor/rules/03-ui.mdc

==============================================

조건-피그마) 활용할MCP: FigmaMCP 채널 : 9fuv5m1e
조건-피그마) 결제 완료 모달 참고 노드ID: 214:1721
            
조건-피그마) 피그마에서 확인할 사항:
            - 모달 전체 레이아웃 및 크기
            - 아이콘 디자인 (크기, 색상, 배경)
            - 텍스트 스타일 (제목, 부제목, 설명)
            - 버튼 디자인 (크기, 색상, 간격)
            - 패딩, 마진, 간격 값

==============================================

조건-파일경로) 수정할 파일:
            - components/timecapsule-create/components/confirm-modal/index.tsx
            - components/timecapsule-create/components/confirm-modal/styles.ts
            - components/timecapsule-create/components/confirm-modal/constants.ts (필요시)

조건-파일경로) 반드시 사용해야 할 공통 컴포넌트:
            - commons/components/modal/index.tsx (공통 Modal 컴포넌트)
            - commons/components/button/index.tsx (공통 Button 컴포넌트)
            - commons/components/button/DualButton.tsx (공통 DualButton 컴포넌트)

==============================================

조건-디자인시스템) 기존 디자인 시스템 사용
                  - 색상: @commons/constants/color.ts의 Colors 사용
                  - 타이포그래피: @commons/constants/typography.ts의 Typography 사용
                  - 새로운 색상이나 타이포 정의 금지

==============================================

핵심요구사항) 결제 완료 모달(PAYMENT_COMPLETE)의 UI를 피그마 디자인에 맞게 수정하고,
             공통 Modal 컴포넌트와 공통 Button 컴포넌트를 사용하여 일관성 있는 UI를 구현할 것.

            1) 현재 아키텍처 이해
                A) 공통 Modal 컴포넌트 (commons/components/modal)
                   - CRITICAL: 이미 구현되어 있고, 프로젝트에서 사용 중임
                   - ConfirmModal은 이 공통 Modal의 children으로 전달됨
                   - 사용 예시 (components/toss-payments/index.tsx:86-91):
                     ```typescript
                     openModal({
                       width: 344,
                       height: 'auto',
                       closeOnBackdropPress: true,
                       children: <ConfirmModal type="PAYMENT_COMPLETE" onConfirm={handlePaymentCompleteConfirm} />,
                     });
                     ```
                   - 즉, ConfirmModal은 모달의 "내용"만 담당하고, 실제 모달 기능은 공통 Modal이 제공

                B) ConfirmModal 컴포넌트
                   - 모달 "내용"만 렌더링 (아이콘, 텍스트, 버튼)
                   - Modal의 children으로 전달됨
                   - 3가지 타입(PAYMENT_COMPLETE, SUBMIT_CONFIRM, SUBMIT_COMPLETE)을 지원

                C) 현재 문제점
                   - 버튼이 Pressable로 직접 구현되어 있음 (index.tsx:106-127)
                   - 공통 Button 컴포넌트를 사용하지 않아 일관성 부족
                   - 피그마 디자인과 차이가 있을 수 있음

            2) 수정 방향

                A) 피그마 디자인 확인
                   - CRITICAL: 먼저 피그마 노드(214:1721)를 확인하여 정확한 디자인 파악
                   - 확인할 사항:
                     - 모달 크기 (width, height) - openModal 호출 시 설정
                     - 모달 내용 패딩 (paddingVertical, paddingHorizontal)
                     - 아이콘 영역 (크기, 색상, 배경색, 배경 크기)
                     - 텍스트 영역 (제목, 부제목, 설명의 스타일, 간격)
                     - 버튼 영역 (버튼 크기, 색상, variant, 전체 너비 사용 여부)
                     - 전체 요소 간 간격

                B) 공통 Button 컴포넌트로 교체
                   - CRITICAL: commons/components/button의 Button, DualButton 컴포넌트 반드시 사용
                   - Pressable로 직접 구현한 버튼 완전히 제거
                   - 버튼 스타일은 공통 컴포넌트에서 관리하므로 styles.ts의 버튼 관련 스타일 제거

                C) 3가지 모달 타입별 버튼 설정
                   - PAYMENT_COMPLETE (결제완료):
                     - 버튼 1개 (확인 버튼)
                     - Button 컴포넌트 사용
                     - variant: 'primary'
                     - size: 피그마에서 확인 (예: 'L', 'M', 'S' 중 선택)
                     - fullWidth: true

                   - SUBMIT_CONFIRM (제출 재확인):
                     - 버튼 2개 (취소 + 제출)
                     - DualButton 컴포넌트 사용
                     - size: 피그마에서 확인
                     - cancelLabel: constants에서 정의한 취소 버튼 텍스트
                     - confirmLabel: constants에서 정의한 확인 버튼 텍스트

                   - SUBMIT_COMPLETE (제출완료):
                     - 버튼 1개 (확인 버튼)
                     - Button 컴포넌트 사용
                     - variant: 'primary'
                     - size: 피그마에서 확인
                     - fullWidth: true

            3) 구현 단계

                1단계: 피그마 노드 확인
                   - 214:1721 노드를 확인하여 PAYMENT_COMPLETE 모달의 디자인 값 추출
                   - 모달 크기, 아이콘, 텍스트, 버튼의 모든 디자인 속성 확인
                   - 각 요소 간 간격 (spacing) 정확히 측정
                   - 피그마 디자인을 Colors, Typography에 매칭

                2단계: Constants 업데이트
                   - MODAL_CONTENTS에 buttonSize 추가
                   - 피그마에서 확인한 버튼 크기 ('L', 'M', 'S' 중 선택)
                   - 예시:
                     ```typescript
                     import { Colors } from '@/commons/constants/color';
                     import { IconName } from 'react-native-remix-icon';

                     export const MODAL_CONTENTS = {
                       PAYMENT_COMPLETE: {
                         title: '결제가 완료되었습니다',
                         subtitle: '타임캡슐이 생성되었습니다',
                         iconName: 'checkbox-circle-fill' as IconName,
                         iconColor: Colors.green[500], // 피그마에서 확인
                         iconBackgroundColor: Colors.green[50], // 피그마에서 확인
                         confirmText: '확인',
                         buttonCount: 1 as const,
                         buttonSize: 'M' as const, // 피그마에서 확인한 버튼 크기
                         closeOnBackdropPress: true,
                       },
                       SUBMIT_CONFIRM: {
                         title: '타임캡슐을 제출하시겠습니까?',
                         description: '제출 후에는 수정할 수 없습니다',
                         iconName: 'alert-fill' as IconName,
                         iconColor: Colors.orange[500], // 피그마에서 확인
                         iconBackgroundColor: Colors.orange[50], // 피그마에서 확인
                         confirmText: '제출',
                         cancelText: '취소',
                         buttonCount: 2 as const,
                         buttonSize: 'M' as const, // 피그마에서 확인
                         closeOnBackdropPress: false,
                       },
                       SUBMIT_COMPLETE: {
                         title: '제출이 완료되었습니다',
                         subtitle: '타임캡슐이 성공적으로 제출되었습니다',
                         iconName: 'checkbox-circle-fill' as IconName,
                         iconColor: Colors.green[500], // 피그마에서 확인
                         iconBackgroundColor: Colors.green[50], // 피그마에서 확인
                         confirmText: '확인',
                         buttonCount: 1 as const,
                         buttonSize: 'M' as const, // 피그마에서 확인
                         closeOnBackdropPress: true,
                       },
                     } as const;
                     ```

                3단계: 버튼 컴포넌트 import
                   - components/timecapsule-create/components/confirm-modal/index.tsx 파일 수정
                   - 공통 Button, DualButton import 추가
                     ```typescript
                     import { Button } from '@/commons/components/button';
                     import { DualButton } from '@/commons/components/button/DualButton';
                     ```

                4단계: 버튼 렌더링 로직 수정
                   - 기존 Pressable 버튼을 공통 버튼 컴포넌트로 교체

                   - 1개 버튼 (PAYMENT_COMPLETE, SUBMIT_COMPLETE):
                     ```typescript
                     {config.buttonCount === 1 ? (
                       <Button
                         label={config.confirmText}
                         variant="primary"
                         size={config.buttonSize}
                         fullWidth={true}
                         onPress={handleConfirm}
                       />
                     ) : (
                       // ...
                     )}
                     ```

                   - 2개 버튼 (SUBMIT_CONFIRM):
                     ```typescript
                     {config.buttonCount === 1 ? (
                       // ...
                     ) : (
                       <DualButton
                         cancelLabel={config.cancelText || '취소'}
                         confirmLabel={config.confirmText}
                         size={config.buttonSize}
                         onCancelPress={handleCancel}
                         onConfirmPress={handleConfirm}
                       />
                     )}
                     ```

                5단계: Styles 정리
                   - styles.ts에서 버튼 관련 스타일 제거
                   - 제거할 스타일:
                     - button
                     - buttonPrimary
                     - buttonSecondary
                     - buttonHalf
                     - buttonRow (DualButton이 자체적으로 처리)
                     - buttonText
                     - buttonTextPrimary
                     - buttonTextSecondary

                   - 유지할 스타일:
                     - buttonContainer (버튼을 감싸는 컨테이너, 상단 여백만 관리)
                     - container, iconContainer, textContainer 등

                6단계: 피그마 디자인 적용
                   - 피그마에서 확인한 모든 값을 styles.ts에 반영
                   - 모달 내용 패딩 (paddingVertical, paddingHorizontal)
                   - 아이콘 크기 및 배경 크기
                   - 텍스트 스타일 (Typography 매칭)
                   - 요소 간 간격 (marginBottom 등)
                   - Colors, Typography를 사용하여 색상 및 텍스트 스타일 적용

                7단계: 사용처에서 모달 호출 확인
                   - components/toss-payments/index.tsx 등에서 openModal 호출 시
                   - 피그마에서 확인한 모달 크기(width, height) 적용
                   - 예시:
                     ```typescript
                     openModal({
                       width: 344, // 피그마에서 확인한 값
                       height: 'auto',
                       closeOnBackdropPress: true,
                       children: <ConfirmModal type="PAYMENT_COMPLETE" onConfirm={handlePaymentCompleteConfirm} />,
                     });
                     ```

==============================================


주의사항

1. 공통 컴포넌트 사용 필수
   - CRITICAL: 공통 Modal 컴포넌트(commons/components/modal) 사용
   - CRITICAL: 공통 Button/DualButton 컴포넌트(commons/components/button) 사용
   - ConfirmModal은 Modal의 children으로 전달됨
   - 직접 구현한 버튼 코드 완전히 제거

2. 피그마 디자인 정확히 따르기
   - CRITICAL: 반드시 피그마 노드(214:1721)를 확인
   - 모든 디자인 값(크기, 색상, 간격)을 피그마에서 가져올 것
   - Colors, Typography를 사용하여 색상 및 텍스트 스타일 매칭
   - 새로운 색상 코드나 타이포 스타일 직접 정의 금지

3. 아키텍처 이해
   - 공통 Modal: 모달 기능 제공 (backdrop, 닫기, 애니메이션 등)
   - ConfirmModal: 모달 내용만 렌더링 (아이콘, 텍스트, 버튼)
   - 공통 Button/DualButton: 버튼 스타일 및 동작 제공

4. 기존 기능 유지
   - 3가지 모달 타입(PAYMENT_COMPLETE, SUBMIT_CONFIRM, SUBMIT_COMPLETE) 모두 정상 작동
   - onConfirm, onCancel 콜백 정상 작동
   - renderInfoCard 등 기존 로직 유지

5. 타입 안정성
   - constants에 buttonSize 타입 추가
   - ModalContentConfig 타입에 buttonSize 추가
   - ButtonSize = 'L' | 'M' | 'S' 타입 활용

6. 코드 일관성
   - 프로젝트 전체에서 모달은 공통 Modal 컴포넌트 사용
   - 프로젝트 전체에서 버튼은 공통 Button/DualButton 컴포넌트 사용
   - 스타일은 공통 컴포넌트에서 관리

==============================================

최종검토 체크리스트

- [ ] 공통 Modal 컴포넌트(commons/components/modal) 사용하고 있음을 확인함
- [ ] ConfirmModal이 Modal의 children으로 전달됨을 확인함
- [ ] 피그마 노드(214:1721) 확인하여 디자인 값 추출함
- [ ] 모달 크기(width, height)를 피그마에서 확인함
- [ ] 모달 내용 패딩, 간격을 피그마에서 확인함
- [ ] 아이콘, 텍스트, 버튼의 모든 디자인 속성 확인함
- [ ] constants.ts에 buttonSize 추가함 (피그마에서 확인한 크기)
- [ ] Button, DualButton import 추가함
- [ ] PAYMENT_COMPLETE 모달의 버튼을 공통 Button 컴포넌트로 교체함
- [ ] SUBMIT_CONFIRM 모달의 버튼을 공통 DualButton 컴포넌트로 교체함
- [ ] SUBMIT_COMPLETE 모달의 버튼을 공통 Button 컴포넌트로 교체함
- [ ] Pressable로 구현한 기존 버튼 코드 완전히 제거함
- [ ] styles.ts에서 버튼 관련 스타일 모두 제거함
- [ ] 피그마에서 확인한 패딩, 간격 값을 styles.ts에 적용함
- [ ] Colors, Typography를 사용하여 색상 및 텍스트 스타일 적용함
- [ ] 새로운 색상 코드나 타이포 스타일을 직접 정의하지 않았음
- [ ] 3가지 모달 타입 모두 정상 작동 확인
- [ ] 버튼 1개 모달 정상 작동 (PAYMENT_COMPLETE, SUBMIT_COMPLETE)
- [ ] 버튼 2개 모달 정상 작동 (SUBMIT_CONFIRM)
- [ ] onConfirm, onCancel 콜백 정상 작동
- [ ] renderInfoCard 등 기존 로직 정상 작동
- [ ] openModal 호출 시 피그마에서 확인한 모달 크기 적용함
- [ ] TypeScript 타입 에러 0건
- [ ] 코드 가독성 및 일관성 확보
- [ ] 실제 결제 완료 시나리오에서 모달 테스트 완료

==============================================

추가 고려사항

1) 모달 크기 일관성
   - 각 모달 타입별로 적절한 크기 사용
   - openModal 호출 시 width, height 피그마 값으로 설정

2) 버튼 크기 일관성
   - 모든 모달에서 동일한 버튼 크기 사용 권장 (예: 'M')
   - 특별한 이유가 없다면 일관된 크기로 통일

3) DualButton 간격
   - DualButton 컴포넌트가 자체적으로 버튼 간격 관리
   - 추가 스타일 불필요

4) 접근성
   - 공통 Button 컴포넌트가 접근성 속성 자동 제공
   - accessibilityRole, accessibilityLabel 등

5) 애니메이션
   - 공통 Modal 컴포넌트가 모달 애니메이션 제공
   - 공통 Button 컴포넌트가 press 상태 피드백 제공

6) 확장성
   - 새로운 모달 타입 추가 시 constants에만 추가
   - 버튼 스타일은 공통 컴포넌트에서 관리하므로 유지보수 용이

==============================================
