import { View, Text } from 'react-native';
import { styles } from './styles';

export const WriteBottomsheet = (): JSX.Element => {
  return (
    <View style={styles.container}>
      <Text>Write Bottomsheet Component</Text>
    </View>
  );
};
