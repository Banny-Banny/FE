import { View, Text } from 'react-native';
import { styles } from './styles';

export const StepInfo = (): JSX.Element => {
  return (
    <View style={styles.container}>
      <Text>Step Info Component</Text>
    </View>
  );
};
