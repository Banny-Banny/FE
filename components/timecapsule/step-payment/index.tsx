import { View, Text } from 'react-native';
import { styles } from './styles';

export const StepPayment = (): JSX.Element => {
  return (
    <View style={styles.container}>
      <Text>Step Payment Component</Text>
    </View>
  );
};
