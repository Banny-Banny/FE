/**
 * components/customer-service/components/file-picker/styles.ts
 * 파일 선택 컴포넌트 스타일 정의
 */

import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  // 파일 피커는 모달이나 액션 시트로 표시되므로
  // 여기서는 기본 스타일만 정의
  container: {
    // 필요시 추가
  },
});
