/**
 * components/customer-service/components/message-status/styles.ts
 * 메시지 상태 컴포넌트 스타일 정의
 */

import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  messageStatusContainer: {
    // 상태 아이콘 컨테이너는 추가 스타일 불필요
  },
});
