/**
 * components/onboarding/components/login-step/email-login/types.ts
 * 이메일 로그인/회원가입 폼 타입 정의
 * 
 * Zod 스키마에서 추론된 타입을 re-export
 */

export type { LoginFormData, SignupFormData } from './schemas';
